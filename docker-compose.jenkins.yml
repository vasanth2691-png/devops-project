services:
  jenkins:
    image: jenkins/jenkins:lts-jdk17
    container_name: devops-sample-jenkins
    ports:
      - '8081:8080'
      - '50000:50000'
    volumes:
      - jenkins_home:/var/jenkins_home

volumes:
  jenkins_home:
